# Migrations

Generate migrations to match the PRD schema (users, students, teachers, managers, levels, classes, subjects, specializations, lessons, lesson_media, quizzes, monthly_tests, papers_work, attendance, teacher_time_table, track_teachers, fees, payments, class_activities, student_guidance, marks).

Example:
```
php artisan make:migration create_users_table
```
